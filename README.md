# Forge

Personal fitness tracking web app for your data, your trends.

## Local setup

1. Clone the repository
2. Run `npm install`
3. Copy `.env.example` to `.env.local` and fill in your Firebase credentials from the Firebase Console
4. Run `npm run dev`

The app will be available at http://localhost:5173.
